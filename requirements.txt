# Install with:  pip install -r requirements.txt
#
# Raspberry Pi note: PySide6 may not be available via pip on armhf/arm64.
# If pip fails, install the system package instead:
#   sudo apt install python3-pyside6.qtquick python3-pyside6.qtqml \
#                    python3-pyside6.qtwidgets python3-pyside6.qtcharts

# Qt6 Python bindings — includes QtQuick, QtQml, QtCharts, QtWidgets, QtMultimedia
PySide6

# USB serial communication (Waveshare ESP32, Feather GPS)
pyserial
