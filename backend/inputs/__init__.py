# Input handler modules.  Each reads from a hardware source and pushes
# values into DashBackend.
#
#   esp32_serial.py  — RPM, gear, lockup status, overdrive status from ESP32
#   gnss_sensor.py   — ground speed from a USB GNSS receiver
